version: '3.9'

services:

  malayanma:

    build: .

    container_name: malayanma_ai

    ports:
      - "5000:5000"

    volumes:
      - .:/app

    env_file:
      - .env

    restart: unless-stopped